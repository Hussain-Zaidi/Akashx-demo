import React from "react";
import { Metadata } from "next";
import Page from "../cognitive";

export const metadata: Metadata = {
  title: "Ontlogy Views",
  description: "World’s #1 OLCP Database.",
};

export default function OntlogyViewsPage() {
  return <Page />;
}
